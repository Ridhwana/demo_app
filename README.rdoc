-- Ruby on Rails --

Chapter 2:

The application utilises the scaffolding approach to create a web application.
It also uses Rake to migrate the database (sqlite).
To view the web application the following URL's are used: /users and /microposts
Also run the commands bundle install and bundle exec rake db:migrate before running the server.
